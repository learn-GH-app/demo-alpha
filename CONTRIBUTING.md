# Contributing

This repository exists so that a GitHub App has something to read. It is not a
real project, and there is nothing to contribute.

It is here because `gh-app-demo audit` looks for it, and a report where every
row is identical demonstrates nothing.
